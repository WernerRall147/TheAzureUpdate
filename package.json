{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "posts": "node scripts/generate-manifest.mjs",
    "prestart": "node scripts/generate-manifest.mjs",
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "playwright_test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "sirv-cli": "^3.0.1"
  }
}
